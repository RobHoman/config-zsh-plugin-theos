echo "Loading $0..."

# Contains configuration settings and aliases for a typical
# osx installation of the theos IDE

export THEOS=/opt/theos
export THEOS_DEVICE_IP=192.168.0.7 THEOS_DEVICE_PORT=22

# Add cycript to the path
export PATH=$PATH:/usr/local/Cycript_0.9.502
